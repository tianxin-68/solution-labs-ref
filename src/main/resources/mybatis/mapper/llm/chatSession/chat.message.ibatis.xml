<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chat.message">

    <insert id="insertChatMessage" parameterType="com.bi.queryer.ssm.llm.chatSession.entity.ChatMessage"
            useGeneratedKeys="true" keyProperty="chatMessageId" keyColumn="chat_message_id">
        insert into chat_message (
            chat_message_type,
            is_summarized,
            chat_id,
            chat_data_id,
            question_content,
            answer_content,
            answer_duration_seconds,
            answer_status,
            ai_model,
            chat_begin_time,
            chat_end_time,
            created_by,
            created_time
        ) values (
            #{chatMessageType},
            #{isSummarized},
            #{chatId},
            #{chatDataId},
            #{questionContent},
            #{answerContent},
            #{answerDurationSeconds},
            #{answerStatus},
            #{aiModel},
            #{chatBeginTime},
            #{chatEndTime},
            #{createdBy},
            #{createdTime}
        )
    </insert>

    <select id="listChatMessageByChatId" parameterType="map" resultType="com.bi.queryer.ssm.llm.chatSession.entity.ChatMessage">
        select
            chat_message_id as chatMessageId,
            chat_message_type as chatMessageType,
            is_summarized as isSummarized,
            chat_id as chatId,
            chat_data_id as chatDataId,
            question_content as questionContent,
            answer_content as answerContent,
            answer_duration_seconds as answerDurationSeconds,
            answer_status as answerStatus,
            ai_model as aiModel,
            chat_begin_time as chatBeginTime,
            chat_end_time as chatEndTime,
            created_by as createdBy,
            created_time as createdTime
        from chat_message
        where chat_id = #{chatId}
        order by chat_message_id asc
    </select>

    <select id="listChatMessageByIds" parameterType="map" resultType="com.bi.queryer.ssm.llm.chatSession.entity.ChatMessage">
        select
            chat_message_id as chatMessageId,
            chat_message_type as chatMessageType,
            is_summarized as isSummarized,
            chat_id as chatId,
            chat_data_id as chatDataId,
            question_content as questionContent,
            answer_content as answerContent,
            answer_duration_seconds as answerDurationSeconds,
            answer_status as answerStatus,
            ai_model as aiModel,
            chat_begin_time as chatBeginTime,
            chat_end_time as chatEndTime,
            created_by as createdBy,
            created_time as createdTime
        from chat_message
        where chat_message_id in
        <foreach item="item" index="index" collection="messageIds" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <select id="getChatMessageById" parameterType="long" resultType="com.bi.queryer.ssm.llm.chatSession.entity.ChatMessage">
        select
            chat_message_id as chatMessageId,
            chat_message_type as chatMessageType,
            is_summarized as isSummarized,
            chat_id as chatId,
            chat_data_id as chatDataId,
            question_content as questionContent,
            answer_content as answerContent,
            answer_duration_seconds as answerDurationSeconds,
            answer_status as answerStatus,
            ai_model as aiModel,
            chat_begin_time as chatBeginTime,
            chat_end_time as chatEndTime,
            created_by as createdBy,
            created_time as createdTime
        from chat_message
        where chat_message_id = #{chatMessageId}
    </select>

    <update id="batchUpdateIsSummarized" parameterType="map">
        update chat_message
        set is_summarized = #{isSummarized}
        where chat_id = #{chatId}
          and chat_message_id in
        <foreach item="item" index="index" collection="messageIds" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>

    <update id="batchUpdateIsSummarizedByIds" parameterType="map">
        update chat_message
        set is_summarized = #{isSummarized}
        where chat_message_id in
        <foreach item="item" index="index" collection="messageIds" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>

</mapper>
