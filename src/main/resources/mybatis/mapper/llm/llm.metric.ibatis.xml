<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="llm.metric">

    <select id="queryAll" resultType="com.bi.queryer.ssm.llm.entity.agent.LLMAgentMetric">
        select
            metric_code as metricCode,
            metric_name as metricName,
            metric_config as metricConfig
        from
            wf_adapte_metric
    </select>

    <select id="getMetricCodeWithWfConfig" resultType="String">
        SELECT
            DISTINCT
            t2.metric_code
        FROM
            wf_base t1
                INNER JOIN wf_adapte_metric t2
                           ON t1.id = t2.`wf_id`
        WHERE t1.publish_status = 'published'
          AND is_active = 1
    </select>

</mapper>