<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssm.metric.expansion.mapping">

    <!-- 按源指标编码查询全部目标映射 -->
    <select id="listBySourceMetricCode" parameterType="java.lang.String"
            resultType="com.bi.queryer.ssm.migrate.bizsplit.model.MetricExpansionMappingEntity">
        SELECT
            pkid AS pkid,
            source_metric_code AS sourceMetricCode,
            target_metric_code AS targetMetricCode,
            source_businessline AS sourceBusinessline,
            target_businessline AS targetBusinessline
        FROM
            ssm_metric_expansion_mapping
        WHERE
            source_metric_code = #{sourceMetricCode}
        ORDER BY
            pkid ASC
    </select>

    <!-- 查询全部映射（历史/兜底；主流程改用 listByBusinessline） -->
    <select id="listAll" resultType="com.bi.queryer.ssm.migrate.bizsplit.model.MetricExpansionMappingEntity">
        SELECT
            pkid AS pkid,
            source_metric_code AS sourceMetricCode,
            target_metric_code AS targetMetricCode,
            source_businessline AS sourceBusinessline,
            target_businessline AS targetBusinessline
        FROM
            ssm_metric_expansion_mapping
        ORDER BY
            pkid ASC
    </select>

    <!--
      按源业务线加载映射（主流程入口）。
      sourceBusinessline 必填；targetBusinessline 可选——门户替换指定时追加过滤，
      未指定时查该源业务线下全部，再由服务层按视图 owner 的 DEPT_ID 收窄。
    -->
    <select id="listByBusinessline" parameterType="java.util.Map"
            resultType="com.bi.queryer.ssm.migrate.bizsplit.model.MetricExpansionMappingEntity">
        SELECT
            pkid AS pkid,
            source_metric_code AS sourceMetricCode,
            target_metric_code AS targetMetricCode,
            source_businessline AS sourceBusinessline,
            target_businessline AS targetBusinessline
        FROM
            ssm_metric_expansion_mapping
        WHERE
            source_businessline = #{sourceBusinessline}
        <if test="targetBusinessline != null and targetBusinessline != ''">
            AND target_businessline = #{targetBusinessline}
        </if>
        ORDER BY
            pkid ASC
    </select>

</mapper>
