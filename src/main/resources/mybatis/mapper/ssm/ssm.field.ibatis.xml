<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssm.field">
    <select id="getFieldAuthBlackList" parameterType="java.util.HashMap" resultType="com.bi.queryer.ssm.meta.MetaFieldAuthBlackList">
        select
            t.user_name as userName,
            t.field_group as fieldGroup,
            t.field_code as fieldCode,
            t.field_title as fieldTitle
        from bi_ssm.ssm_field_auth_blacklist t
    </select>
    <select id="getFieldAuthWhiteList" parameterType="java.util.HashMap" resultType="com.bi.queryer.ssm.meta.MetaFieldAuthWhiteList">
        select
            t.user_name as userName,
            t.field_group as fieldGroup,
            t.field_code as fieldCode,
            t.field_title as fieldTitle
        from bi_ssm.ssm_field_auth_whitelist t
    </select>

    <!-- 按创建人删除字段权限白名单 -->
    <delete id="deleteFieldAuthWhiteListByCreatedBy" parameterType="String">
        DELETE FROM ssm_field_auth_whitelist WHERE created_by = #{createdBy}
    </delete>

    <!-- 批量插入字段权限白名单 -->
    <insert id="batchInsertFieldAuthWhiteList" parameterType="java.util.List">
        INSERT INTO ssm_field_auth_whitelist (pkid, user_name, field_code, field_title, field_group, created_by, created_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.pkid}, #{item.userName}, #{item.fieldCode}, #{item.fieldTitle}, #{item.fieldGroup}, #{item.createdBy}, now())
        </foreach>
    </insert>
</mapper>
