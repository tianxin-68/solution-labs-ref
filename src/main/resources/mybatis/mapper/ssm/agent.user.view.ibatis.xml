<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssm.agent.user.view">

    <select id="queryViewListByUserName" parameterType="String" resultType="com.bi.queryer.ssm.api.entity.AgentUserViewEntity">
        SELECT
            a.view_id   AS viewId,
            a.view_name AS viewName,
            a.view_desc AS viewDesc,
            a.sort_id   AS sortId
        FROM
            ssm_user_agent_view a
        WHERE
            a.user_name = #{userName}
            AND a.is_active = 1
        ORDER BY
            a.sort_id ASC
    </select>

    <!-- 检查 viewId 是否在 ssm_user_agent_view 中 -->
    <select id="existsViewInAgentView" parameterType="String" resultType="int">
        SELECT COUNT(1)
        FROM ssm_user_agent_view
        WHERE view_id = #{viewId}
          AND is_active = 1
    </select>

    <!-- 删除 ssm_user_agent_view_config 中指定 viewId 的所有配置 -->
    <delete id="deleteConfigByViewId" parameterType="String">
        DELETE FROM ssm_user_agent_view_config
        WHERE view_id = #{viewId}
    </delete>

    <!-- 批量插入 ssm_user_agent_view_config -->
    <insert id="batchInsertConfig" parameterType="java.util.List">
        INSERT INTO ssm_user_agent_view_config (pkid, view_id, column_name, column_area, created_time, updated_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.pkid}, #{item.viewId}, #{item.columnName}, #{item.columnArea}, NOW(), NOW())
        </foreach>
    </insert>

    <select id="queryConfigByViewIds" parameterType="java.util.List" resultType="com.bi.queryer.ssm.api.entity.AgentUserViewConfigEntity">
        SELECT
            view_id     AS viewId,
            column_name AS columnName,
            column_area AS columnArea
        FROM
            ssm_user_agent_view_config
        WHERE
            view_id IN
            <foreach collection="list" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
    </select>

</mapper>
