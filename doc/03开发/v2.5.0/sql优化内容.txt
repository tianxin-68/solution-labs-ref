1、order by 从 select中调整到order by子句中，只有lod表达式放在select子句中
2、布尔类型值去掉case when，调整到结果输出时转换
3、周/月/年日期过滤时，调整为日过滤，去掉日期截取过滤，便于走查询时走分区
 - 调整为过滤日
 - check-mtd
 - 月份结果展示为yyyy-MM
 - lod
 - 保有量等非日粒度指标

权限修复：行总计，指标无权限时对应的行总计也无权限