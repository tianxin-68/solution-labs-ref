当前项目说明：当前是一个java spingmvc的工程，用于构建多维查询相关功能，其核心能力是按照用户在前端界面选择的维度、指标、以及分析计算（同环比、lod计算等）生成doris、trino的sql，最终将sql提交查询并返回给前端用户。

需求：现在多维查询查询的sql构建中添加个新的聚合方式“日方差”

“日方差”计算逻辑： 先按日对明细数据按原始聚合方式计算后，再次进行方差计算。计算方式类似现在的”日均值“的计算逻辑。

现状问题："日均值”相关逻辑代码主要在com.bi.queryer.ssm.engine.aggregation中，但系统中有很多地方使用或通过AggExpressionType.Avg_By_Day或一些硬代码判断了是否是日均的逻辑

目标：帮我规划个修改或重构的方案
- 新增一个聚合方式AggExpressionType.var_pop_by_d，对应到doris的方差函数是var_pop
- 重构后，支持当前日均值的计算，同时后面可扩展先按”日“汇总后需再次计算的场景，如：此次新增的“日方差”
- 给出重构的逻辑和原因
- 列处涉及到需要修改的地方以及原因

注意：此次重构的项目是一个长期需要迭代的项目，需要重点考虑其兼容性、可扩展性、可维护性

输出：将方案写入到doc/02设计/日方差-系统重构方案.md